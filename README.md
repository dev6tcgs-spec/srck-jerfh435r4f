# 🎄 Московская зимняя ярмарка

Telegram-бот с инлайн-кнопками, где все взаимодействие происходит через редактирование одного сообщения — никаких спамов, всё плавно как в приложении.

## 🎯 Описание

Ты — организатор московской зимней ярмарки. Открывай павильоны, выполняй мини-задания (обслуживай посетителей), зарабатывай мандаринки 🍊 и собирай коллекцию интересных фактов о Москве.

## 🚀 Установка и запуск

### 1. Клонируйте репозиторий или скачайте файлы

### 2. Установите зависимости

```bash
pip install -r requirements.txt
```

### 3. Получите токен бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и получите токен

### 4. Настройте переменные окружения

Создайте файл `.env` в корне проекта:

```env
BOT_TOKEN=ваш_токен_бота
```

Или скопируйте `.env.example`:

```bash
cp .env.example .env
```

И отредактируйте `.env`, вставив свой токен.

### 5. Запустите бота

```bash
python bot.py
```

Бот автоматически:
- Создаст базу данных `winter_fair.db`
- Загрузит все игровые данные (павильоны, задания, факты)
- Будет готов к работе!

## 📁 Структура проекта

```
zimamos/
├── bot.py              # Основной файл бота
├── database.py         # Работа с базой данных
├── game_data.py        # Игровые данные (павильоны, задания, факты)
├── tasks_handler.py    # Обработчики заданий разных типов
├── config.py           # Конфигурация
├── requirements.txt    # Зависимости
├── .env.example       # Пример файла с переменными окружения
└── README.md          # Этот файл
```

## 🎮 Игровые механики

### Типы заданий:

1. **Реакция на время** — нажми кнопку в нужный момент
2. **Выбор из вариантов** — выбери правильный вариант
3. **Многошаговый процесс** — выполни несколько шагов подряд

### Павильоны:

- 🚂 **Поезд** (0🍊) — стартовый павильон
- ⛸️ **Коньки** (300🍊)
- 🧊 **Холодильник** (500🍊)
- 🍊 **Мандарин** (700🍊)
- 🍬 **Коробка конфет** (1,000🍊)
- 🫖 **Самовар** (1,500🍊)
- 🎁 **Фабрика подарков** (2,500🍊)

### Система фактов:

- Всего 75 фактов о Москве
- Факты открываются после выполнения заданий
- Собранные факты сохраняются в коллекции

## 🛠 Технические детали

- **Язык:** Python 3.8+
- **Библиотека:** python-telegram-bot 20.7
- **База данных:** SQLite (aiosqlite)
- **Архитектура:** Асинхронная

## 📝 Команды бота

- `/start` — начать игру или вернуться в главное меню

## 🎨 Особенности

- Все взаимодействие через редактирование одного сообщения
- Плавные переходы между экранами
- Интуитивный интерфейс с эмодзи
- Система прогрессии и коллекционирования

## 🔧 Разработка

Для разработки и тестирования:

1. Создайте тестового бота через @BotFather
2. Используйте токен тестового бота в `.env`
3. Запустите бота локально
4. Тестируйте функционал

## 🚀 Деплой на VDS

**Обновление кода:**
- Через Git: `git push` → на VDS: `git pull && ./update.sh`
- Через скрипт: отредактируйте `sync_to_vds.ps1` и запустите → на VDS: `./update.sh`

**Мониторинг:**
- `./monitor.sh` - статус и логи
- `tail -f logs/errors.log` - ошибки

## 📄 Лицензия

Проект создан для образовательных целей.

## 🎄 Приятной игры!

Если возникли вопросы или проблемы, создайте issue в репозитории.
