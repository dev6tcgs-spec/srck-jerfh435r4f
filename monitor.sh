#!/bin/bash
# Скрипт для мониторинга бота

cd /path/to/zimamos  # ЗАМЕНИТЕ на ваш путь!

echo "📊 Статус бота:"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

# Проверка процесса
if pgrep -f "python.*bot.py" > /dev/null; then
    PID=$(pgrep -f "python.*bot.py")
    echo "✅ Бот запущен (PID: $PID)"
else
    echo "❌ Бот НЕ запущен!"
    exit 1
fi

echo ""
echo "📝 Последние логи (20 строк):"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
tail -n 20 logs/bot_$(date +%Y-%m-%d).log 2>/dev/null || echo "Логи не найдены"

echo ""
echo "❌ Последние ошибки (10 строк):"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
tail -n 10 logs/errors.log 2>/dev/null || echo "Ошибок нет"

echo ""
echo "💾 Размер логов:"
du -sh logs/* 2>/dev/null | tail -5

