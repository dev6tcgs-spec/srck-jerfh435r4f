# 🚀 Инструкция по деплою на VDS

## 📋 Подготовка

### 1. Подключение к VDS

```bash
ssh user@your-vds-ip
```

### 2. Установка зависимостей на сервере

```bash
# Обновление системы
sudo apt update && sudo apt upgrade -y

# Установка Python и pip
sudo apt install python3 python3-pip python3-venv -y

# Создание виртуального окружения (опционально, но рекомендуется)
python3 -m venv venv
source venv/bin/activate
```

### 3. Загрузка проекта на VDS

**Вариант А: Через Git (рекомендуется)**
```bash
cd ~
git clone your-repo-url zimamos
cd zimamos
```

**Вариант Б: Через SCP**
```bash
# На вашем локальном компьютере
scp -r zimamos user@your-vds-ip:/home/user/
```

**Вариант В: Через SFTP (FileZilla, WinSCP и т.д.)**

### 4. Настройка проекта

```bash
cd ~/zimamos

# Установка зависимостей
pip install -r requirements.txt

# Создание .env файла
nano .env
# Добавьте: BOT_TOKEN=ваш_токен_здесь

# Создание директории для логов
mkdir -p logs
```

### 5. Настройка скриптов деплоя

Отредактируйте `deploy.sh` и `update.sh`:
- Замените `/path/to/zimamos` на реальный путь к проекту
- Убедитесь, что пути к Python правильные

Сделайте скрипты исполняемыми:
```bash
chmod +x deploy.sh update.sh monitor.sh
```

## 🚀 Первый запуск

```bash
./deploy.sh
```

Или вручную:
```bash
python3 bot.py
```

## 🔄 Обновление бота

### Способ 1: Через скрипт (быстро)
```bash
./update.sh
```

### Способ 2: Вручную
```bash
# Остановить бота
pkill -f "python.*bot.py"

# Обновить код (если используете git)
git pull origin main

# Обновить зависимости
pip install -r requirements.txt

# Запустить заново
nohup python3 bot.py > logs/bot_stdout.log 2> logs/bot_stderr.log &
```

## 📊 Мониторинг

### Проверка статуса
```bash
./monitor.sh
```

### Просмотр логов в реальном времени
```bash
# Все логи
tail -f logs/bot_$(date +%Y-%m-%d).log

# Только ошибки
tail -f logs/errors.log

# Стандартный вывод
tail -f logs/bot_stdout.log

# Стандартные ошибки
tail -f logs/bot_stderr.log
```

### Поиск ошибок
```bash
# Найти все ошибки за сегодня
grep ERROR logs/bot_$(date +%Y-%m-%d).log

# Найти ошибки за последний час
grep ERROR logs/errors.log | tail -20
```

## 🔧 Настройка автозапуска (systemd)

Следуйте инструкциям в файле `systemd_service.txt`

После настройки:
```bash
# Проверка статуса
sudo systemctl status winter-fair-bot

# Перезапуск
sudo systemctl restart winter-fair-bot

# Просмотр логов
sudo journalctl -u winter-fair-bot -f
```

## 🐛 Отладка проблем

### Бот не запускается

1. Проверьте токен:
```bash
cat .env | grep BOT_TOKEN
```

2. Проверьте логи ошибок:
```bash
cat logs/bot_stderr.log
cat logs/errors.log
```

3. Проверьте, что порты открыты (если используете прокси)

### Бот падает

1. Проверьте логи:
```bash
tail -100 logs/errors.log
```

2. Проверьте использование памяти:
```bash
ps aux | grep python
```

3. Проверьте диск:
```bash
df -h
```

### Обновления не применяются

1. Убедитесь, что бот перезапущен:
```bash
pkill -f "python.*bot.py"
sleep 2
python3 bot.py &
```

2. Проверьте, что файлы обновлены:
```bash
ls -la bot.py
```

## 📝 Структура логов

```
logs/
├── bot_2024-01-15.log    # Логи за день
├── bot_2024-01-16.log    # Логи за следующий день
├── errors.log            # Все ошибки
├── bot_stdout.log        # Стандартный вывод
└── bot_stderr.log        # Стандартные ошибки
```

## 🔐 Безопасность

1. **Не коммитьте .env в git!**
2. Используйте права доступа:
```bash
chmod 600 .env
chmod 755 *.sh
```

3. Регулярно обновляйте зависимости:
```bash
pip list --outdated
pip install --upgrade package_name
```

## 📞 Полезные команды

```bash
# Найти процесс бота
ps aux | grep bot.py

# Убить процесс
pkill -f "python.*bot.py"

# Проверить использование ресурсов
top -p $(pgrep -f "python.*bot.py")

# Очистить старые логи (старше 7 дней)
find logs/ -name "*.log" -mtime +7 -delete
```
